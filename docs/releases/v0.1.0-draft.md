# DevOps Tools Catalogue v0.1.0 - Release Preparation (Draft)

Status: Draft preparation only. Do not tag or publish until explicit owner approval.

## Release title

DevOps Tools Catalogue v0.1.0 - Canonical Model and Deterministic Docs Baseline

## Proposed version

v0.1.0

## Verified functionality

- Canonical source-of-truth model in YAML under `data/tools/`.
- Controlled taxonomy in `data/taxonomy.yaml`.
- JSON Schema validation contract in `schema/tool.schema.json`.
- Deterministic documentation generation (`scripts/generate_docs.py`) with no drift under `--check`.
- Catalogue validation pipeline (`scripts/validate_catalog.py`) including schema, drift, links policy, and hygiene checks.
- Test suite and maintenance code quality checks are green on `main` after dependency updates.
- GitHub Actions workflow job expected by policy remains named `validate`.
- GitHub Actions are pinned to full 40-character SHAs.

## Imported catalogue content (current generated artefacts)

From `docs/catalog-statistics.json` and migration artefacts:

- Canonical records: 1092
- Categories: 30
- Status `needs-review`: 1085
- Status `archived`: 7
- Reconciled source occurrences: 1894
- Reconciliation dispositions: kept 1085, merged 802, archived 7

## Dependency maintenance included before this release draft

Merged via squash to `main`:

- PR #3: pytest range updated to `>=8.3,<10`.
- PR #6: `actions/checkout` updated to v7.0.1 SHA.
- PR #4: `actions/setup-python` updated to v7.0.0 SHA.
- PR #5: `actions/upload-artifact` updated to v7.0.1 SHA.

## Known limitations and ongoing work

- Most imported records remain `needs-review` and require primary-source verification.
- Unresolved URL and lifecycle review remains open in issue #2:
  - https://github.com/goozcena-gnl/Devops-Tools/issues/2
- Repository is private and ruleset enforcement on `main` is limited by current GitHub plan; branch rules may not be enforceable as configured.
- No software license has been selected by owner yet.

## Unresolved URL/lifecycle work statement

Do not bulk-update URLs. Only apply metadata updates with primary-source evidence and preserve decisions in the version-controlled override/reconciliation mechanisms.

## Release verification checklist (pre-tag)

- [ ] `python -m ruff check scripts tests` passes.
- [ ] `python -m ruff format --check scripts tests` passes.
- [ ] `python -m pytest -q` passes.
- [ ] `python -m scripts.generate_docs` and second `--check` pass with no drift.
- [ ] `python -m scripts.validate_catalog` passes.
- [ ] Internal Markdown links pass validation through catalogue checks.
- [ ] Secret-pattern scan and catalogue secret checks pass.
- [ ] Workflow audit confirms full-SHA action pinning.
- [ ] Workflow audit confirms explicit read-only permissions by default.
- [ ] No `pull_request_target` workflow exists.
- [ ] Working tree is clean.
- [ ] Issue #2 remains open and references `main` artefacts.

## Proposed annotated tag command (do not run yet)

```bash
git tag -a v0.1.0 -m "DevOps Tools Catalogue v0.1.0 - Canonical Model and Deterministic Docs Baseline"
git push origin v0.1.0
```

Do not execute the tag commands until explicit owner authorization.
